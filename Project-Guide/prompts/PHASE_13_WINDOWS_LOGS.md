# SentinelAI — Windows Logs Phase Prompt


You are continuing the existing SentinelAI project. Inspect the current repository before editing. Do not modify unrelated pages. Preserve working APIs and routes. Use the uploaded reference image as the visual source of truth. Create only the current page folder. Keep page-specific components, hooks, services, utilities and assets inside it. Keep shared primitives in the shared components directory. Use real backend data, dynamic statuses, loading, empty and error states. Run lint, tests and the frontend build before declaring completion.


## Current page

Rebuild only **Windows Logs**.

Create:

```text
frontend/src/pages/windows-logs/
```

## Required scope

Implement collector state, filters, summary charts, paginated events, details, raw XML, AI explanation and MITRE mapping.

## Visual rule

The uploaded Windows Logs image is the sole page design reference. Recreate its layout, density, proportions, typography hierarchy, color treatment, card sizing and interaction style as closely as practical using real React components. Do not use the screenshot as a background.

## Workflow

1. Inspect existing page, routing, shared layout and relevant APIs.
2. List files to create, replace or patch.
3. Identify missing backend data.
4. Implement safe modular code.
5. Connect real APIs and WebSockets.
6. Run tests, lint and build.
7. Report exact results and remaining limitations.

Do not create folders for future pages.
